---
name: laravel-models
description: How this project writes Eloquent — the Laravel 13 model shape (class attributes rather than the older `$fillable`/`$hidden`/`$casts` properties), casts, enums, where domain logic lives, and migration, factory, and seeder conventions. Use when adding or changing a model, column, migration, or enum, deciding whether logic belongs on the model or in a service, or writing a factory. Read it before writing model code from memory — most Laravel material still shows the pre-13 style.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/backend/laravel/models.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
