---
name: laravel-http-layer
description: How this project builds API endpoints — thin controllers, FormRequests as the validation and authorization seam, API Resources as the outward contract, the status-code table, and the split between the api and web route files. Use when adding or changing an endpoint, writing a controller action, adding validation rules, shaping a JSON response, deciding which route file something belongs in, or debugging a 403, a 422, or a response whose shape a client did not expect.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/backend/laravel/http-layer.md`
- `catalyst/stacks/backend/laravel/models.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
