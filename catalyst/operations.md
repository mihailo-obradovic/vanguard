# Operations Runbook

Per `references/operations-runbook.md`: one section per stateful component — **Operate** / **Recovery** / **Quirks**. Commands and traps only; rules live in `architecture.md` and the feature docs.

## MySQL

Host-local server (no Docker/Sail), database `vanguard` at `127.0.0.1:3306`, credentials in `.env`. Holds application tables plus sessions, cache, and the (currently idle) jobs queue.

### Operate

```bash
php artisan db:show                # connection + table overview
php artisan migrate:status         # which migrations have run
php artisan tinker                 # poke at data
mysql -u <DB_USERNAME> -p vanguard # interactive client
```

One-time test-database provisioning (as MySQL root; `RefreshDatabase` migrates it on each run):

```bash
sudo mariadb -e "CREATE DATABASE IF NOT EXISTS vanguard_testing; GRANT ALL PRIVILEGES ON vanguard_testing.* TO '<DB_USERNAME>'@'localhost'; FLUSH PRIVILEGES;"
```

### Recovery

No backup routine exists — dev-only data so far; a restore drill has never been performed. Recreate from scratch with `php artisan migrate:fresh --seed` (seeder creates `test@example.com` admin). **Before any production use, a dump/restore routine must be added here and rehearsed.**

### Quirks

- Tests run on the separate `vanguard_testing` database on this same server (same engine, per the Laravel testing module): `phpunit.xml` pins `DB_CONNECTION`/`DB_DATABASE`; host and credentials are inherited from `.env` (no `.env.testing` exists — creating one would silently take over the test env, since `APP_ENV=testing` prefers it). `php artisan test --parallel` would need databases `vanguard_testing_1..n` — not provisioned.
- Sessions live in the `sessions` table (`SESSION_DRIVER=database`): truncating it logs everyone out; deleting a user leaves orphaned session rows (no FK).

## Queue

`QUEUE_CONNECTION=sync` in the local `.env` — queued notifications (verification + reset mail) execute inline in the request. The `jobs` table and the `queue:listen` process in `composer run dev` are ready for the `database` driver but idle under `sync`.

### Operate

```bash
php artisan queue:listen --tries=1   # only meaningful with QUEUE_CONNECTION=database
php artisan queue:failed             # inspect failures (database driver)
```

### Recovery

Under `sync` there is nothing to recover — a mail failure surfaces as a request error. Under `database`, failed jobs land in `failed_jobs`; retry with `php artisan queue:retry all`. Never drilled.

### Quirks

- Switching to `database` without running a worker silently strands all auth mail in the `jobs` table.

## Mail

Mailtrap sandbox SMTP (`sandbox.smtp.mailtrap.io:2525`, creds in `.env`) — all mail is captured, nothing reaches real inboxes.

### Operate

Check the Mailtrap inbox for verification/reset mails; `php artisan pail` (part of `composer run dev`) tails mail-send log entries.

### Quirks

- `MAIL_FROM_ADDRESS` is still `hello@example.com`.

## Dev stack

### Operate

```bash
composer run dev    # serve (localhost:8000) + queue:listen + pail + pnpm run dev
php artisan serve   # API alone
pnpm dev            # SPA alone (localhost:3000)
php artisan test && pnpm test   # both suites
composer test:coverage          # backend coverage (sets XDEBUG_MODE=coverage; needs the Xdebug extension)
composer test:coverage-html     # same, plus browsable report into reports/coverage-backend/ (gitignored), opened in the browser
pnpm test:coverage              # frontend coverage (text + html + lcov into coverage/, gitignored)
pnpm test:ui                    # Vitest browser dashboard, watch mode with coverage embedded
composer test:mutation          # backend mutation audit (below)
pnpm test:mutation              # frontend mutation audit (below; preflight-checked)
composer format                 # backend formatter (Pint, laravel preset); composer format:check to verify only
pnpm format                     # frontend formatter (oxfmt); pnpm format:check to verify only
```

Mutation audit (ADR 013; run after a test-writing push, not on a schedule): `composer test:mutation` mutates `app/` (Pest's built-in mutation testing; `--everything` because classes carry no `covers()` annotations, `--covered-only` to skip mutations no test executes) and reruns the covering tests per mutant. **Do not add `--parallel`** — mutant processes share the `vanguard_testing` database and would trample each other. Every listed _untested_ mutation is a change no test noticed — fix the test it exposes or record here why the mutant is acceptable. Score is a measurement, never a gate.

Current figures (re-measure before trusting: `composer test:coverage`, `composer test:mutation`): coverage **100% lines**; mutation **82.06%** (262 mutations, 47 surviving, all triaged below).

Accepted surviving mutants — recheck when touching the code they live in:

- **Events with no listeners** — removing `event(PasswordReset|Verified|Lockout)` is unobservable because nothing consumes them yet; the day a listener lands, its tests kill these.
- **Rule-list residue** (the largest group, ~15): `'sometimes'` on a field with no `required` beside it — Laravel skips non-implicit rules on an absent attribute anyway, so the mutant is behaviorally identical; `'string'` on a field whose format rule already rejects non-strings (`validateEmail` does); `'string'` on the GraphQL validator's `name`, unreachable because the schema types the argument `String`; and `'email'` on `LoginRequest`, where a malformed address fails the credential check into the same 422.
- **Password-broker message copy** (`PasswordResetLinkController` rules and both `!=` → `!==` on the status strings): every path already answers 422 keyed `email`; killing these would mean pinning broker message text, which is copy, not contract.
- **Throttle message copy and key residue** — `minutes` and its `ceil` are never rendered (no `lang/` is published and the framework's `auth.throttle` interpolates only `:seconds`); the `'|'` separator and the concat order leave the key unique per (email, IP) pair either way. The parts that _are_ contractual — the threshold of 5, the seconds in the message, the counter clearing on success, and scoping to both email and client address — are pinned now.
- **`remember_token` rotation** (`Str::random(60)` length and the array item on reset) — a deliberate hold: the `remember` flow is a recorded feature-doc gap, and pinning half of it here would document behavior the contract does not yet claim.
- **Session regeneration in the controllers** (`AuthenticatedSessionController::store`, `RegisteredUserController::store`) — `Auth::login()` already calls `$this->session->regenerate(true)`, so removing the explicit call changes nothing observable. Verified by hand, not assumed.
- **Defensive code unreachable through routes**: `UserRequest::authorize()`'s `&&` (route middleware guarantees a user), `ResourcePayload`'s JSON depth and assoc flags (Lighthouse's default resolver reads arrays and objects alike), `array_intersect_key` unwraps in both `UpdateUser`s (inputs are pre-filtered by both transports' validators).

Frontend mutation audit (ADR 013; `stryker.config.json`, same cadence and triage rule as the backend's). The mutate globs cover the logic layers — `web/utils`, `web/services`, `web/stores`, `web/composables` — plus `web/plugins`, `web/regle-config.ts`, `web/i18n/i18n.config.ts`, and the component surface: `web/components`, `web/layouts` and `web/app.vue`. Pages and middleware stay out — the presentation exception (`decisions/013`): every mutant there would report as noise rather than signal. On `variant/shadcn-vue` the vendored registry source, `web/components/ui/`, stays out too: it is shadcn-vue's code rather than this project's logic, its annotated deviations are classes, and eleven of its files hold compiler macros that would each need a disable pair — edits `shadcn-vue diff` would then report on every run. Nothing else is excluded by name. The untested composables stay in scope on purpose: Stryker separates `NoCoverage` from `Survived` and prints a covered-code score beside the total, which is the honest analogue of the backend's `--covered-only` without a hand-maintained exclusion list that rots.

Two things about `.vue` in particular:

- **Stryker mutates only the `<script>` block.** Template logic is never mutated, so a component whose branching lives in the template — `AuthCard`, whose script is type-only, for instance — scores on its script alone and can read as 0% while being thoroughly tested. The same fact is why several components yield no mutants at all: `SkipLink` and `Empty.vue` have no script, `TheFooter` derives one constant, and master's `Default.vue` is three lines of composable calls. Master's `UIField` is the inverse case: the message it shows is a template expression (`errors?.[0] ?? ''`) and goes unmutated, while the aria wiring its script derives does not. Read a `.vue` score as a statement about its script, not about the component.
- **Compiler macros must be excluded from instrumentation where they are used.** Stryker declares its coverage helpers locally inside the script block, and `withDefaults(defineProps<…>(), { … })`, `defineModel(…, { … })` and `defineOptions({ … })` are hoisted out of `setup()` — referencing the helpers from there is a `@vue/compiler-sfc` compile error, not a warning. Every affected component therefore carries a `// Stryker disable all` / `// Stryker restore all` pair around its macro block; the prop defaults, model options and component options inside go unmutated as a result. Fourteen components need it on `variant/vuetify`; on `master` only `UIField` does, for the `defineOptions({ inheritAttrs: false })` its attribute fall-through needs; on `variant/nuxtui` seven do — the six dialogs' `defineModel('open', …)` and `UFormField`'s `defineOptions`. On `variant/shadcn-vue` five do — `FormDialog`, the three auth dialogs and `UIScrollArea` — outside the excluded vendored `ui/`. Do not drop those pairs when merging in either direction.

  **The failure is not always loud.** On `variant/vuetify` it fails the whole dry run with a message naming only the first file it hit. On `UIField` it did something worse: the compile error hit only the spec that imports the component, that one file silently contributed zero tests, and the run completed reporting **every mutant in the file as survived** — a 0% score that reads as "untested" rather than "not compiled". The tell is the test count in the dry run (267 of 276) and `Ran 267.00 tests per mutant`, where a working `.vue` run attributes a handful. A component whose own spec demonstrably fails when its logic is broken by hand, yet scores 0%, is this, not a weak test.

  `defineModel` fails silently in a third way: its instrumented arguments are hoisted into the module-scope `props:` object, so the component throws `ReferenceError: stryMutAct_9fa48 is not defined` at import time rather than failing to compile. On `variant/nuxtui` that made seven spec files (44 tests) — the six dialog specs and `Default.spec.ts`, whose import chain reaches the dialogs — collect zero tests each, and the dry run reported success on the shrunken set. The tell is the same: compare the dry run's test count against `pnpm test`'s before trusting any figure.

- **Per-test attribution is narrower than coverage.** Stryker runs only the tests it recorded as covering a mutant, and for a mutant inside an `if` condition that set can exclude the tests taking the _other_ branch. `UIDialog`'s focus-trap condition (`event.shiftKey && …`) is reported as survived for exactly that reason: its covering set is the two Shift+Tab specs, both of which pass under it. Forcing the branch by hand turns **four** specs red. Before writing a survivor off as a missing test, break it in the source and run the spec — the covering set in `reports/mutation/frontend.json` says which tests the score actually reflects.

Accepted surviving mutants on `master`'s components — recheck when touching the code they live in (the two user form dialogs' entries hold on `variant/shadcn-vue` too, where the same reset guard, `clearExternalErrors`, empty-form fallback and `props.user?.id` survivors recur):

- **`UIDialog`'s panel null guards** (`panel.value?.querySelectorAll`, `panel.value?.focus()`, and the `?? []` beside the first, which is reported as uncovered) — the panel exists whenever the handlers can run: they are bound to it. Defensive, unobservable. The _opener_ guards are no longer on this list — `opener?.isConnected` and the `?.` on the `#main-content` lookup are both killed by the two dead-opener specs.
- **`UIDialog`'s `if (!first || !last) return`** (both the condition and `||` → `&&`) — with nothing focusable inside, `first` and `last` are both `undefined` and the two `active === …` comparisons below match nothing anyway, so the guard changes no outcome. It stays because it states the intent and stops the queries.
- **`UIDialog`'s Shift+Tab condition** — the attribution artifact above, verified by hand rather than assumed.
- **`UserGqlFormDialog`'s empty-form fallbacks** (`user?.name ?? ''` and its two siblings) — `user === null` is what closes that dialog, so those values only ever feed a form nobody can see; the visible ones come from the watcher's reseed.
- **`UserFormDialog`'s `props.user?.id`** — the getter only runs from the availability rule, which is `try`/`catch` fail-open by design (feature 008), so the `TypeError` the mutant causes in create mode is swallowed into "available" exactly like a network failure. Worth knowing: that catch masks programming errors as well as outages.
- **All three form dialogs' reset guards** (`if (!open) return`, and `UserGqlFormDialog`'s `if (!user) return`) — resetting on the way closed as well as open changes nothing: the content unmounts in between, so resetting on either edge leaves the same form on the next open. (In `ProfileFormDialog` even the inverted `if (open) return` survives, for the same reason.)
- **All three form dialogs' `clearExternalErrors: true`** — prescribed by `stacks/frontend/nuxt/validation.md` and kept for that reason, but unobservable here: `$reset` alone hides the verdict (Regle keeps errors off a pristine field), and editing the field clears the entry from the bridge's ref before any resubmit can surface it. Tested from the user's side instead — "does not bring the last attempt's verdict back on the next submit" — which is the behaviour, whichever line delivers it.

`pnpm test:mutation` runs `scripts/preflight-mutation.mjs` before Stryker. It exists because the two ways this run breaks both report something unrelated to the cause: a missing `.nuxt/tsconfig.app.json` surfaces as a bare "No tests were found", and a `node_modules/.modules.yaml` pointing into the sandbox surfaces as every later pnpm command demanding to purge `node_modules`. The preflight names each one and prints the fix. Both branches of it were proven by breaking the tree on purpose, not by reading the code.

One more trap, which no preflight can catch: **a crashed run still leaves the previous report in place**, and piping the command to something like `tail` hides the non-zero exit. The tell is a rerun that reproduces the previous score exactly — check `reports/mutation/frontend.json`'s modification time before believing it.

Four config choices are load-bearing and easy to undo by accident:

- **`ignorePatterns` is only `!.nuxt`.** Stryker honors `.gitignore`, so nothing else needs listing — excluding the Laravel directories on top was measured and changed neither the score nor the wall clock (488K of copying). The negation is the whole point of the option here: because `.nuxt` is gitignored it is not copied into the sandbox, and the partial tree that `buildNuxt` regenerates there has no `tsconfig.app.json`, the root `tsconfig.json` references it, and every spec then dies in oxc with `TSCONFIG_ERROR`. Stryker reports that as a bare "No tests were found".
- **No `packageManager` key.** Setting it makes Stryker run `pnpm install` inside the sandbox, where `node_modules` is a symlink back to the real one — pnpm rewrites the real `node_modules/.modules.yaml` to point `virtualStoreDir` at the sandbox, and every later pnpm command in the project demands to purge `node_modules`. Never run pnpm with a working directory inside `.stryker-tmp` either; the recovery is `CI=true pnpm install --frozen-lockfile` from the project root.
- **`ignoreStatic: true`** — `web/mocks/setup.ts` swaps `globalThis.$fetch` at module scope, and a static mutant forces a full environment reload per mutant against that load-order-sensitive capture.
- **`vitest.related: false`** — related-file resolution across Nuxt's vite pipeline can silently fail to find the covering tests, which surfaces as a whole file reported `Survived`. Per-test coverage already selects the right specs.

`concurrency: 4` is a portable default, not a tuned one. Measured on 16 cores: 4 → 1m32, 8 → 1m20, 12 → 1m30. Raising it toward half the core count is worth a measurement on a given machine; Stryker's own default (`cores - 1`) is past the knee, because each worker boots its own Nuxt vite server. `disableTypeChecks` was measured at its default versus `false` — identical score, no time saved — so it stays untouched.

Anything that puts a second copy of this repository inside the working tree — a crashed Stryker sandbox, a `git worktree` under `.claude/worktrees/` — gets collected by vitest as if it belonged here, and every spec runs twice, once against the wrong tree. Both are excluded in `vitest.config.ts`; a sudden doubling of the test-file count is that failure, not a broken change.

Current figures, measured on the merged trees (re-measure before trusting: `pnpm test:mutation`): `master` **92.76%** total / **95.60%** on covered code (539 mutants); `variant/vuetify` **82.98%** / **89.34%** (858 mutants); `variant/nuxtui` **78.19%** / **84.91%** (885 scored mutants, survivors not yet triaged — see that branch's `decisions/014`); `variant/shadcn-vue` **89.54%** / **91.39%** (1,090 scored mutants, vendored `ui/` excluded; every survivor triaged below). **The branches' totals are not comparable**: the same globs run over very different surfaces — master's `.vue` contribution is 20 mutants across three files, the variant's is several hundred across nineteen. Compare the covered-code score, or compare per file. A corollary proven here twice: a template-only feature moves the number not at all (the table's loading states arrived with four specs and left the score identical to the mutant), so read the score as a statement about scripts, never about the app.

Stryker reports several mutants per line, so before writing a test against a survivor, read its `replacement` in `reports/mutation/frontend.json` — a guard's outer condition and its inner operand are separate mutants, and sabotaging the wrong one proves nothing.

Accepted surviving mutants — recheck when touching the code they live in:

- **The `typeof x !== 'object'` operand** in `getValidationErrors` and `gqlFetcher`'s `toValidationBody` — reachable only for a truthy non-object, and every such value already yields `{}` through the string-array message filter below it. The guard is defense-in-depth the filter provides anyway.
- **`RedirectDecision.reason`** (all three strings) — written and never read anywhere in `web/`; diagnostic metadata, not behavior. The day something displays or logs it, its test kills these.
- **`.some` → `.every` on `guestOnlyPrefixes`** — the list holds one prefix, so the two are equivalent; a second prefix makes this killable and should arrive with the test that adds it. (Master only — the variant has no prefix list.)
- **`credentials: 'include'` in `fetcher`** — MSW intercepts below the point where credentials mode is observable, so no test at this layer can see it. The browser enforces it, and the session feature covers the effect.
- **The empty `default:` branch** in `handleApiError` — a Stryker artifact on a `break` with no behavior.
- **`sameAs(password, 'password')`'s second argument** — the field label inside the message, not a contract; same reasoning as the backend's copy mutants.
- **`parseResponse`'s `console.error` text** — `conventions/testing.md` prohibits asserting on logs.
- **`auth-loader`'s `catch` block** — deleting `resetUser()` changes nothing: the store is always empty when the plugin boots, so the reset is defensive against a state the app cannot be in.
- **`useCookieConsent`'s `maxAge` arithmetic** (two mutants) — a cookie's lifetime is not readable back through `document.cookie`; the browser enforces it.
- **`'declined'` → `''`** — both values mean "decided, and not consented" to every reader of the composable, which is the only thing that reads the cookie. Killable the day something else reads the stored string.
- **`app.vue`'s `useHead` block** (7 mutants — the title, the favicon link, the object and array around them) — document metadata is copy, not contract.
- **`app.vue`'s `decision.shouldRedirect && decision.redirectTo`** — the second arm is unreachable through `authRedirectLogic`, which never returns a redirect decision without a target; a type-narrowing guard covered by its own spec.
- **`CookieConsentBanner`'s `isMounted` initial value** — a flash guard for the first client frame; under `ssr: false` there is no hydration pass for it to protect against, so no assertion at this level can see it.
- **`app.vue`'s `'guest'`** → `''` (`variant/shadcn-vue`) — `determineAuthRedirect` compares the session only against `'signed-in'`, so every other string is a guest. Equivalent.
- **`layouts/Default.vue` residue** (`variant/shadcn-vue`) — the `NAV_LINK` class string, and the `?.` in `menuButton.value?.$el` on both drawer hand-offs: the menu button renders at every width (only CSS hides it at `lg`), so the ref is set whenever the drawer can be open.
- **`UserCard`'s optional-chaining fallbacks** (`variant/shadcn-vue`; `user.value?.name || ''`, its email twin, `user.value?.id`) for a `user` the store cannot hold as null while the card renders.
- **`UserCard`'s `() => editMode.value`** (`variant/shadcn-vue`) — reported survived, but forcing it to `true` by hand turns "does not submit on Enter while the profile is only being read" red: the per-test attribution artifact above.
- **`const form = ref({…})` → `{}`** in `ForgotPasswordDialog` and `RegisterDialog` (`variant/shadcn-vue`) — the fields render an absent key and an empty string identically through `v-model`, and Regle's `required` refuses both. `LoginDialog` has no such survivor, because its initial value is the development credentials and the spec asserts them.
- **`handleOpenChange`'s `!open`** → `true` in both user form dialogs (`variant/shadcn-vue`) — the dialog's open state is the page's prop, so Reka only ever emits the close edge; there is no `update:open(true)` for the inverted branch to act on. `UserFormDialog`'s `'set'` → `''` beside it is equivalent too: `newPasswordRules` treats anything but `'change'` as setting.
- **`UserDeleteDialog`'s `if (!props.user) return`** (`variant/shadcn-vue`) — the confirm button exists only while there is a user to name, so the guard cannot be reached. Defensive.
- **`LocaleSwitcher`'s model getter and `?.name`** (`variant/shadcn-vue`) — the getter feeds the Reka listbox's selected option, which only renders in the open listbox that happy-dom cannot drive (the register row below); the trigger's text is passed explicitly and stays covered. The active locale is always one of the configured ones, so the `?.` is defensive.
- **`useFocusReturn`'s `opener?.isConnected`** and **`useMutationDialog`'s `instances[mode.value]?.error`** (`variant/shadcn-vue`) — close-auto-focus only fires after an open, which always records an opener; and `mode` only ever names an instance the map was built with. Defensive.
- **`UIScrollArea`, all 15** (`variant/shadcn-vue`) — the wiring the register row below leaves to the browser walk: under happy-dom every scroll metric is `0`, so no edge ever appears and nothing about the refs, the initial state, the guard or the observer list is observable. The arithmetic behind it is `scrollEdges`, tested directly.
- **`regle-config.ts`, all 16, in the 2026-09-13 run only** (`variant/shadcn-vue`) — reported survived with its own spec attributed zero coverage, where the run an hour earlier on the same code killed all 16; breaking the required, maxLength and sameAs messages by hand turns `regle-config.spec.ts` red. The config is read once per app boot, so which spec Stryker credits with covering it varies by worker. Re-run before believing a drop here.
- **Survivors in files identical to master's** (`variant/shadcn-vue`: `emailRules.ts` 8, `scrollEdges.ts` 4, `newPasswordRules.ts`'s default `'set'`) — a test added only here is dropped by the next sync (master wins `web/utils/`), so their triage is master's audit, tracked in Workflowy ("master mutation audit: emailRules + scrollEdges survivors"). The `scrollEdges` four are killable — the exact 1px tolerance boundary is untested — and so, likely, are two of `emailRules`'.

### Vendored shadcn-vue components

`variant/shadcn-vue` only. Renovate sees `reka-ui` and the other packages behind `web/components/ui/`, never the vendored files themselves, so upstream changes to those reach this project only through a deliberate check. Run it when Renovate raises a `reka-ui` bump, and before adding a component to a folder that is already vendored.

```bash
git status --short                                           # must be clean: the check overwrites files
pnpm dlx shadcn-vue@latest add <component…> --overwrite --yes  # every vendored folder under web/components/ui/
pnpm exec oxfmt web/components/ui                            # drops the registry's formatting from the diff
git diff -- web/components/ui                                # read it
git checkout -- web/components/ui                            # then restore, and re-apply only what is worth taking
```

Every hunk left after the format pass is one of two things: a deviation this project made, which must carry its `Default:`/`Changes:` annotation (an unannotated one is a bug to fix, not a hunk to shrug at), or an upstream change to take by hand. Measured 2026-09-13 on `field` (pulling `label` and `separator`): the only hunks left were `FieldError`'s annotated reserved height and the braces the lint requires.

**Not `shadcn-vue diff`.** In 2.8.2 it reports `No updates found` for every component here, including `FieldError`, which is known to differ. The cause is in its `diffComponent`: the registry lists each file as `ui/<component>/<File>.vue` and the CLI joins that onto the resolved `ui` alias, `web/components/ui`, so it looks for `web/components/ui/ui/…`, finds nothing, and skips every file silently. Re-check on a CLI upgrade before trusting it again.

Two CLI traps, both on any `add`:

- **`main.css`'s comment is rewritten.** Every `add` re-serializes the CSS entry and turns the literal `<style scoped>` in its comment into `\3c style scoped>`. Repair that one hunk after the last `add`; never `git checkout` the whole file, which also throws away whatever the `add` was meant to write there. It did not fire for `sheet`, `collapsible` or the check above — read the diff anyway.
- **`--yes` does not answer the overwrite prompt.** It skips only the confirmation; an `add` that meets an existing file still stops and asks. Pass `--overwrite` to take upstream's file, or pipe `yes n |` to keep yours.

### Browser walk, 2026-09-03 — the scroll and layout rules

The half that cannot be unit-tested, measured in Chromium at 393x760 DPR 2.625 and 760x393 landscape, against the real API. Redo this walk when either rule's implementation changes.

- **Per-edge scroll rules behave.** The users table reported `edge-bottom` at the top of its scroll (top border `rgba(0,0,0,0)`), both edges in the middle, and `edge-top` alone at the end — the state a single region-level boolean gets wrong at both ends. The colour resolved to `rgb(134, 142, 150)`, master's `--color-scroll-edge` token (on `variant/shadcn-vue` the rule is `--scroll-edge`, Dracula `#6272a4` light and `#939fbf` dark). Both border widths read 1px in every state, so only the colour changes and no edge toggle moves content.
- **The dialog bug was real, and it is landscape that shows it.** With the panel unbounded, as it was before this change, the create-user dialog rendered 781px tall in a 393px-high viewport with its submit button 169px below the fold and `document.documentElement` unable to scroll — genuinely unreachable. Bounded, the panel is 361px, the body scrolls, and the button is reachable. In **portrait** English the same panel overflowed by only 10px and the button stayed on screen, so a portrait-only check would have found nothing. Walk this one in landscape.
- **The two-line error reservation is the right size, measured rather than chosen** (master's `UIField`; on `variant/shadcn-vue` the same reservation is an annotated deviation inside the vendored `ui/field/FieldError`, which upstream unmounts when valid). In `sr-Cyrl` the email format message renders on **two** lines at a 311px box width inside the dialog. The reservation resolved to 36.4px (2 x 18.2), and introducing the error changed no field height and grew the dialog's content by 0. The previous one-line reservation would have grown that field by a line and pushed every field below it, plus the submit button.
- **Reserved labels hold the widest variant.** Each `UIReservedLabel` box measured equal to its widest ghost rather than its shown text — logout holds 115.9px while showing the shorter `Одјава`, and the role column holds 106.4px whether the row reads `Корисник` or `Администратор`, which is what stops an auto-layout table re-widthing on a role change. Accessible names carry the active variant only.
- **Correction to the table-split commit.** That commit's message says the sticky column headers cover the scroll region's top edge rule. They do not: the sticky `th` begins 1px _inside_ the container's border box, so the rule paints above the header band and is visible when scrolled. Kept as it is rather than suppressed — the blue band is present whether or not content is hidden above, so it signals nothing on its own, and the rule does. (`variant/shadcn-vue` decided the other way on 2026-09-13: its tables scroll in a plain overflow region with no edge rules, the bordered shell and header band counting as the adjacent chrome `scroll-affordance.md` exempts.)
- **Not exercised:** the 1px comparison tolerance. Chromium returned integral scroll metrics at DPR 2.625, so the fractional case the tolerance exists for did not arise live; it is covered in `utils/_tests/scrollEdges.spec.ts` instead.

### Layout stability — what is reserved, and what deliberately is not

`stacks/frontend/_common/layout-stability.md` asks that a state-dependent size change be decided rather than left, so the decisions that went the other way are recorded here instead of being invisible. Reserved cases need no entry: they are the rule being followed, and `UIReservedLabel` is where they land.

| Element                                                                           | Shape                                                                              | Why nothing is reserved                                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `components/_shared/AuthCard.vue` submit button                                   | copy varies by state (`Login` / `Logging in...`, and the three other auth screens) | The button is a block child of a column flex, so it is full-width and its own width moves nothing. The widest variant is roughly half the content box even at 320px, so it never wraps and the height never changes. Shape present, zero measured jump.                                                                                  |
| `pages/users.vue` total count                                                     | value grows a digit                                                                | It is the last inline content in its own paragraph — nothing sits to its right to be pushed. Give it a reserved slot only if a suffix is ever added after it.                                                                                                                                                                            |
| `pages/profile.vue` verification badge                                            | copy varies by state (`Verified` / `Not Verified`)                                 | Its row is `justify-content: space-between`, so the badge grows into the gap between it and the resend control rather than displacing anything. The control beside it _is_ reserved — it was the element giving the row its height.                                                                                                      |
| `components/_shared/CookieConsentBanner.vue`                                      | a block that mounts and unmounts, with its own dismiss controls inside it          | `position: fixed`, so it is out of flow and mounting moves nothing. This is the case the rule calls the worst — a control inside the block it removes — landing safe for a reason unrelated to the control. Its slide transition still owes a `prefers-reduced-motion` path.                                                             |
| `layouts/Default.vue` admin nav links, auth button group                          | blocks that mount on session identity                                              | They mount on who is signed in, not on a control the user operates on that screen, and both sit in horizontal flex rows with nothing below them.                                                                                                                                                                                         |
| `pages/users.vue`, `pages/graphql-demo.vue`, `pages/profile.vue` loading branches | a block that mounts and unmounts                                                   | Not reserved yet, and not exempt — the loading panel and the loaded content are different heights. There is no control here at all: the swap is driven by data arriving, so the rule's "does the control sit above the block" test does not decide it. A skeleton occupying the loaded region is the fix. **Outstanding**, not declined. |

### The app shell, plugins and shared components — what is tested and what is not

Recorded because the alternative is invisible: a file at 0% looks the same whether the omission was deliberate or forgotten. This register is the every-0%-is-legible meta-rule in practice (`conventions/testing.md`, What is deliberately not tested, bound by `stacks/frontend/nuxt/testing.md`): logic gets a test, presentation is proven by the live browser walk, and every deliberate omission names its exception.

The `Branch` column says where the file exists. `both` means the two branches carry it and the decision is the same on each; where the numbers differ, master's is given first.

| File                                                         | Branch  | Lines      | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------ | ------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `plugins/auth-loader.ts`                                     | both    | 100%       | **Tested.** Load-bearing session restore; a failure means a returning user appears signed out on refresh.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `app.vue`                                                    | both    | 87.5%      | **Tested.** The `isLoggedIn` watcher is the only redirect path for a session change with no navigation — logout is a navbar button that never leaves the page, and an expired session resets the store mid-read. The shell around it is markup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `composables/useCookieConsent.ts`                            | both    | 100%       | **Tested.** Cookie-backed persistence with two computeds and two actions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `composables/useExternalErrors.ts`                           | both    | 100%       | **Tested.** Writing the test its own comment implied found the bug that it shared the source object instead of copying it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `composables/useValidationErrors.ts`                         | both    | 100%       | **Tested.** Including the empty map it must yield before anything has failed, which is what a form binds to on first render.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `i18n/i18n.config.ts`                                        | both    | 100%       | **Tested.** The three-branch Slavic plural rule, and the teens exception it exists for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `regle-config.ts`                                            | both    | 100%       | **Tested.** The message catalog every form's validation text comes from.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `components/_shared/CookieConsentBanner.vue`                 | both    | 100%       | **Tested.** Branching render state plus its own persistence — the case `testing.md` names as earning a mounted test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `components/_shared/LocaleSwitcher.vue`                      | master  | 100%       | **Tested.** It calls `setLocale` specifically to persist the choice; asserting the rendered language would not catch losing it. On this branch it is a Reka listbox; see the variant row below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `layouts/Default.vue`                                        | both    | 100% / 98% | **Tested.** Decides what a guest, a user and an admin each see, owns logout, and publishes the focusable `#main-content` landmark that `SkipLink` jumps to and that a closing dialog falls back to (master's `UIDialog`; `useFocusReturn` here) — pinned because nothing in either consumer can tell that the `tabindex` went missing. The admin entries are a convenience, not the control — the API authorizes those pages itself — but showing them advertises a door the user cannot open. On this branch it also owns the three auth dialogs and the phone-width drawer: the hand-offs between dialogs, where a finished login, registration or reset request leads, the drawer closing before it hands off or logs out, and focus returning to the control that started a dialog chain. Which of the bar and the drawer is visible is Tailwind's breakpoint, which happy-dom does not load — a browser-walk check.                                                                     |
| `components/_shared/TheFooter.vue`                           | both    | 100%       | **No test of its own, deliberately.** Presentational; `testing.md` names it as the counter-example. Its 100% is incidental — the layout spec renders it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `components/_shared/UIReservedLabel.vue`                     | both    | 100%       | **Tested.** Its whole job is that the inactive variants stay rendered — a change that dropped one would give the space back and reintroduce the jump, silently and without a visual tell on the machine it was written on. The spec pins that, and that the ghosts are `aria-hidden` so a control does not read out all of its states.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `components/_shared/UIScrollArea.vue`                        | both    | n/a / 92%  | **Arithmetic tested, wiring on the browser walk, deliberately.** The per-edge maths is a pure function with its own spec (`utils/_tests/scrollEdges.spec.ts`), including the two mistakes `scroll-affordance.md` names — the both-edges shortcut and the missing 1px tolerance. The wiring is not honestly unit-testable and is not faked into looking so: under happy-dom `scrollHeight` and `clientHeight` are both `0`, so the region never reports overflow and a green test would prove nothing about the running component. Walk it instead at the narrowest supported width, each region scrolled to the top, the middle and the end — three states checked separately, on a real touch device where an overlay scrollbar will not cover for a border that never appears. On this branch it also bounds `FormDialog`'s body, whose specs execute the wiring (hence the 92%) without being able to observe it — its 15 mutants all survive for the same reason and are recorded above. |
| `components/_shared/LocaleSwitcher.vue`                      | variant | n/a        | **Contract tested, listbox on the browser walk, deliberately.** The component's own job — every configured locale offered under its own name, the active one named on the trigger, and the pick writing through `setLocale` so the detection cookie persists — is specced at the component seam. The listbox itself is not honestly unit-testable and is not faked into looking so: probed under happy-dom, clicking the trigger renders zero options, so a click-driven test would assert against an empty list and pass for the wrong reason. Walk it live: open it, pick each locale, reload and confirm the choice survived.                                                                                                                                                                                                                                                                                                                                                             |
| `components/_shared/SkipLink.vue`                            | both    | n/a        | **No test, deliberately.** No script block at all: an anchor whose entire behaviour is a CSS `:focus` rule moving it on screen. Its target's focusability is pinned in the layout's spec instead. The keyboard path is an accessibility case for the live browser walk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `error.vue`                                                  | master  | 0%         | **No test, deliberately.** Eight lines rendering the error object into the layout — nothing to assert that is not the template itself. Live browser walk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `error.vue`                                                  | variant | n/a        | **Tested** (`_tests/error.spec.ts`, feature 011). Title and message are chosen by status and must never echo the error's own text, and the details must carry exactly three fields — rules, not wiring. `layouts/Empty.vue` has no logic and is proven by the walk (short landscape screen: the card scrolls into reach).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `components/ui/**`                                           | variant | n/a        | **No tests, deliberately, and outside the mutation run.** Vendored shadcn-vue registry source, owned but not written here: its behaviour is Reka's and its styling is classes. The project's deviations inside it are annotated `Default:`/`Changes:` and reviewed through `shadcn-vue diff`, not specs. What the vendored parts are composed into is tested — `FormDialog`, the dialogs, the layout. `Sonner.vue` is the extreme case: a `Toaster` passthrough whose `$toast` seam is fully specced and whose rendered toast is a browser-walk check.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `plugins/hide-devtools-webcomponents.client.ts`              | both    | 12.5%      | **No test, deliberately.** Dev-only DOM housekeeping guarded by `import.meta.dev`; it never runs in production or under test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `middleware/auth.global.ts`                                  | both    | 0%         | **No test, deliberately.** A nine-line thin adapter (`stacks/frontend/nuxt/routing.md` pattern): it calls `determineAuthRedirect` and follows the answer with `navigateTo`. The decision logic lives in `utils/authRedirectLogic.ts` and is unit-tested in `authRedirectLogic.spec.ts`; the wiring itself is unobservable outside a running router — exception 3, proven on the live browser walk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `pages/**`                                                   | both    | 0%         | **No tests, deliberately.** Pages stay on the live browser walk per `decisions/013`; the logic they used to hold has been extracted into the layers above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `components/_shared/FormDialog.vue`                          | variant | 100%       | **Tested.** The base every form dialog is built on: content and slot fallbacks, cancel and confirm carried out to the owner, confirmation disabled while told to or loading, and the Enter contract — confirming from a plain field, not from a control that handles Enter itself, and not at all while confirmation is disabled or loading. Its `after-leave` re-emit is not observable here (see the list below).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `components/_shared/ColorModeToggle.vue`                     | variant | 100%       | **Tested.** The pressed state in each face and the choice persisted under the key the colour-mode head script reads before first paint — both directions, since a toggle that saved only `dark` would reset a returning visitor to the system face. The `dark` class on `<html>` is stamped by that head script, which never runs under test: a browser-walk check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `components/users/{Login,Register,ForgotPassword}Dialog.vue` | variant | 96-100%    | **Tested.** Each dialog's own rules, every invalid field described by its message, the payload it emits, the 422 landing on its field, and the hand-off links that close it before emitting — the layout spec covers the wiring around them, these cover what happens inside.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `components/users/UserCard.vue`                              | variant | 100%       | **Tested.** The densest component here: edit mode, Escape cancelling from anywhere, Enter submitting only while editing, the payload that omits the password pair unless one is being set (the backend reads a present password as a change request), a refused new password described by its messages, the success and resend toasts, and a 422 kept on its field without a toast.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `components/users/UserFormDialog.vue`                        | variant | 100%       | **Tested.** Seeding from the subject and re-seeding on the next one, the password rules switching with the mode and describing a missing pair, the payload that omits the password pair unless one was set, the role picker's label, a 422 landing on its field, and every dismissal — Cancel and the dialog's own Escape — asking the page to close.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `components/users/UserGqlFormDialog.vue`                     | variant | 100%       | **Tested.** The GraphQL edit form: the subject doubling as the open state, the partial payload (`changedFields`), a changed role and the picker naming it, re-seeding on the next subject, the rules refusing a bad submit, a 422 landing on its field, and both dismissals asking to close.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `components/users/UserDeleteDialog.vue`                      | variant | 100%       | **Tested.** The regression it was extracted for — confirming asks for the delete and stays open until the owner settles it — plus Cancel, no dismissal while the delete is in flight, and where focus goes: back to the row button after a cancel, to `#main-content` once a confirmed delete has removed that row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `components/_shared/AuthCard.vue`                            | both    | 100%       | **Tested.** The shell the standalone auth screens are made of (all four on master; `password-reset` alone here): the title, the hint, the fields and footer slots, and the submit control — asking the page to submit, renaming itself while the request is in flight, and refusing a press while submitting or while the form is invalid. Contributes **no mutants**: its script is a type-only `defineProps` plus `defineEmits`, and every branch lives in the template.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `components/_shared/RoleBadge.vue`                           | both    | 100%       | **Tested.** Two cases, and they earn their place: the badge owns the `users.roles.*` lookup its three call sites used to repeat, so the copy is its behaviour. The size variant is a CSS class and is not asserted (`testing.md`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `components/_shared/VerificationBadge.vue`                   | both    | n/a        | **No test, deliberately.** Everything assertable about it is a CSS class: the wording is the caller's (a table column says "Yes"/"No" where the profile view says "Verified"), and the verified state only picks a colour.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `layouts/Empty.vue`                                          | variant | n/a        | **No test, deliberately.** No script block: a centring wrapper around a slot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `spa-loading-template.html`                                  | variant | n/a        | **No test, deliberately.** Static pre-mount HTML outside the Vue app — the boot splash Nuxt injects before hydration. Vitest never loads it; the live browser walk is where it is seen at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `components/profile/ProfileFormDialog.vue`                   | master  | 100%       | **Tested.** The change-password half of the profile contract, none of which was reachable in `profile.vue`: the payload carrying only name and email until a new password is set and the `current_password` challenge with it once one is, the challenge being required by the client rather than by a 422, a mismatched confirmation, `ignore_id` so keeping your own address is not "taken", and a reopen dropping what was typed last time. Mutation 91.89%; survivors triaged above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `components/users/UserGqlFormDialog.vue`                     | master  | 100%       | **Tested.** The GraphQL edit form: the subject doubling as the open state, the partial payload (`changedFields`) sending only what changed — including the id alone when nothing was touched — a changed role, re-seeding on the next subject, the rules refusing a bad submit, and a 422 landing on its field. Mutation 84.38%; survivors triaged above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `components/users/UserFormDialog.vue`                        | master  | 100%       | **Tested.** The create/edit form lifted out of `users.vue`, where none of this was reachable: seeding from the subject (and re-seeding on the next one rather than keeping the last), the payload that omits the password pair unless one was set and includes it when it was, the guard that refuses an invalid submit, `ignore_id` reaching the availability check so an edited user's own address is not "taken", the mode-dependent title and copy, and a 422 landing on its field. Mutation 95.38%; the three survivors are triaged above.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `components/_shared/UIDialog.vue`                            | master  | 100%       | **Tested.** The keyboard contract the four hand-rolled dialogs never had: focus moving into the panel on open and back to the opener on close, Escape and overlay-click asking to close while a click inside does not, and the Tab cycle — wrapping at both edges, left alone in the middle, skipping a disabled control, and bowing out when nothing inside is focusable. Restoration's dead-opener path is covered both ways: focus lands on the `#main-content` landmark the layout publishes, and a document without one is left alone rather than crashing. Mutation 85.71%; the survivors are triaged above.                                                                                                                                                                                                                                                                                                                                                                           |
| `components/_shared/UIDialogActions.vue`                     | master  | n/a        | **No test, deliberately.** No script block: a styled row around a slot, split out of `UIDialog` only so a form dialog's submit button stays inside its `<form>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `components/_shared/UIField.vue`                             | master  | 100%       | **Tested.** The field primitive every form is built from: the generated `id` pairing label to control (distinct per field, which is what the hand-written ids used to get wrong), the first message of the `$errors` array, the `aria-invalid`/`aria-describedby` wiring appearing and disappearing with it, attributes reaching the control rather than the wrapper, and a slotted control (the role select) taking the same wiring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

Four of `variant/shadcn-vue`'s component behaviours are **not observable at this level** and stay on the live browser walk, each recorded where a reader would look for it — in the spec that would otherwise be expected to cover it:

- **`after-leave`** (`FormDialog`'s re-emit, and what the auth dialogs hang on it) — Reka UI's `Presence` finishes a close off the browser's `animationend`, and happy-dom has no CSS animation engine: the dialog closes, but the event never fires.
- **Scroll edges and overflow** (`UIScrollArea`, the users tables' scroll regions) — every scroll metric is `0` without layout.
- **The listbox** (`LocaleSwitcher`, the role pickers) — clicking a Reka `Select` trigger renders zero options under happy-dom, so the specs drive `update:modelValue` at the component seam and name the trigger's label instead.
- **The breakpoint and the colour face** (`layouts/Default.vue`'s bar versus drawer, `ColorModeToggle`'s `dark` class) — Tailwind's `lg:` is CSS happy-dom does not load, and the class is stamped by the colour-mode head script, which never runs under test.

Coverage (re-measure before trusting: `pnpm test:coverage`): `master` **72.68% statements / 73.21% lines**, what remains being almost entirely `pages/`; `variant/vuetify` **77.35% / 77.90%**, what remains being `pages/` at 0%; `variant/shadcn-vue` **77.68% / 79.40%**, what remains being `pages/` and the vendored `components/ui/` parts no composition renders under test. They are close for the same reason: on every branch the logic that used to sit in pages lives in tested components, and what is left uncovered is the pages themselves, presentational vendored or shell code, and the two plugins.

### Quirks

- The SPA and API must run on the exact origins in `SANCTUM_STATEFUL_DOMAINS` / `FRONTEND_URL` (`localhost:3000`/`3001` + `localhost:8000`) or auth silently fails on cookies/CORS.

## CI

GitHub Actions (`.github/workflows/ci.yml`), on push to `master`/`variant/**` and on PRs. Two jobs: frontend (oxlint, oxfmt check, typecheck, vitest with coverage — Node 24 + pnpm) and backend (pint check + Pest with coverage against a `mysql:8` service container whose `MYSQL_DATABASE` is `vanguard_testing`; job-level `DB_*` env vars override the phpunit/.env values). Coverage is printed, never gated (ADR 013); CI's driver is PCOV via `setup-php`, while local runs use Xdebug. No deploy step — deployment remains an honest gap (ADR 001).
