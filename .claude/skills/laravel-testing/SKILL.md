---
name: laravel-testing
description: How this project tests its backend — Pest conventions, feature tests as the primary coverage, factories over seeders, what to cover beyond the happy path, and why the suite runs against the real database engine rather than SQLite. Use when writing or changing a test, deciding what level a behavior should be tested at, adding a regression test for a bug, or setting up the test database.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/backend/laravel/testing.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
